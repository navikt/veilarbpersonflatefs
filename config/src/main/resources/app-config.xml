<?xml version="1.0" encoding="UTF-8"?>
<application xmlns="http://appconfig.aura.nav.no"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://appconfig.aura.nav.no
             http://maven.adeo.no/xsd/appconfig.xsd ">

    <name>veilarbpersonflatefs</name>
    <artifacts>
        <ear groupId="no.nav.fo" artifactId="veilarbpersonflatefs-ear"/>
    </artifacts>

    <selftest>
        <path>veilarbpersonflatefs/internal/selftest</path>
        <humanReadablePath>veilarbpersonflatefs/internal/selftest</humanReadablePath>
    </selftest>

    <resources>
        <rest mapToProperty="iadecorator" alias="iadecorator-js"/>
        <applicationProperties alias="veilarbpersonflatefs.properties"/>
        <baseUrl alias="veilarbpersonPingURL" mapToProperty="veilarbperson.hent_person" />
        <baseUrl alias="veilarbveilederPingURL" mapToProperty="veilarbveileder.hent_enheter" />
        <baseUrl alias="isso-host"/>
        <baseUrl alias="isso-jwks"/>
        <baseUrl alias="isso-issuer"/>
        <credential alias="isso-rp-user"/>

    </resources>

    <serverOptions>
        <memoryParameters resourceAlias="veilarb-memparams"/>
    </serverOptions>
  
    <logging serviceCalls="true"/>
    <loadBalancer isAlive="/veilarbpersonflatefs/internal/isAlive" />
    <security serviceUserResourceAlias="srvveilarbpersonflatefs"/>

</application>